services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: dyp
      POSTGRES_PASSWORD: dyp
      POSTGRES_DB: dyp_farms
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
